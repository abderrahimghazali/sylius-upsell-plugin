{% set fieldName = field|default('triggerProduct') %}
{% set product = attribute(data, fieldName) %}

{% if product is not null %}
    {{ product.name|default('—') }}
{% else %}
    <em class="text-muted">{{ 'upsell.ui.all_products'|trans }}</em>
{% endif %}
